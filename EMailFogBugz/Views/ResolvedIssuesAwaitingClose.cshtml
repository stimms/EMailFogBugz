@{
    _Layout = null;
}
<br />To: @Model.Correspondent, @Model.CustomerEmail
<br />
<br />Regarding the @Model.Project Simplicity Project, you opened a(n) @Model.Priority @Model.Category, titled <a href="https://simplicity-wp.fogbugz.com/default.asp?@Model.ID">@Model.Title</a> on @Model.Opened.Date.ToString("dd MMM yyyy") . 
<br />It was marked <b>@Model.Status</b> as of  @Model.Resolved.Date.ToString("dd MMM yyyy"). It is your responsiblity to test the fix and ensure it is working as expected. 
<br />
<br />The last response added to FogBugz is: 
<br /> @Model.LatestTextSummary

 