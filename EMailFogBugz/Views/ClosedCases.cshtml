@{
    _Layout = null;
}
<br/>Hi, 
you logged an issue with the subject @Model.Title with the Simplicity team regarding the project "@Model.Project".  We classified your issue as a @Model.Priority priority @Model.Category. This is to let you know that we've solved your issue(yay!).

<br/>It was marked <b>@Model.Status</b> as of  @Model.Closed.Date.ToString("dd MMM yyyy"). 
<br/>

<br/>Our comments when closing the issue were:
<pre> 
    @Model.LatestTextSummary
</pre>

 